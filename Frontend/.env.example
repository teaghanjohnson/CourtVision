# Copy to .env.local for local dev.
# In Docker this must be passed as a --build-arg instead -- see the root README's
# "Deploying" section. Setting it only at container runtime has no effect, since
# Next.js inlines NEXT_PUBLIC_* values into the app at build time.
NEXT_PUBLIC_API_URL=http://localhost:8080
